deployment:
  staging:
    branch: develop
    owner: poteto
    commands:
      - ./node_modules/.bin/ember divshot push staging --token $DIVSHOT_TOKEN

  production:
    branch: master
    owner: poteto
    commands:
      - ./node_modules/.bin/ember divshot push staging --token $DIVSHOT_TOKEN
